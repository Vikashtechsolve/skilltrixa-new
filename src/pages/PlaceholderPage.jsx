export default function PlaceholderPage({ title, subtitle }) {
  return (
    <main>
      <div className="page-inner">
        <h1>{title}</h1>
        <p>{subtitle}</p>
      </div>
    </main>
  )
}
